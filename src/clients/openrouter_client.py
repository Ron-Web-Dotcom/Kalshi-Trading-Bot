"""OpenRouter client stub — kept for import compatibility."""

import logging

logger = logging.getLogger("trading.openrouter_client")


class OpenRouterClient:
    """Placeholder — AI decisions now go through src/ai/decision.py using Anthropic directly."""

    def __init__(self):
        pass

    async def close(self):
        pass
